services:
  - type: web
    name: advanced-todo-list   # Change this to your project name
    env: static
    buildCommand: ""       # No build needed for plain HTML/CSS/JS
    staticPublishPath: .   # Publish everything from repo root
    branch: main           # Deploys from your main branch
    plan: free             # Ensures free plan is used
